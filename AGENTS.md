# 项目维护约定

本文件是 Codex、Claude、Gemini 及其他 Agent 的共同入口。先读本文件，再按任务读下方文档；不要依赖某个 Agent 的聊天记录或本机记忆才能操作项目。

## 开始任务

1. 从仓库根目录运行 `git status --short --branch`、`git branch -vv`、`git worktree list`，确认分支、已有改动和其他工作区。主分支为 `main`；需要新分支时默认使用 `codex/` 前缀。
2. 查阅任务涉及的源码和测试。需求与用户明确决定约束实现；描述当前行为时，以当前源码和可复现结果为准，旧计划和历史验收报告不能证明现在的行为或线上状态。
3. 保留用户已有未提交文件、素材删除和其他 Agent 的工作。按文件暂存本次改动，避免 `git add .`、强制重置、清理未跟踪文件或擅自删除其他 worktree。
4. 确认数据目录及外部服务是否隔离，再启动应用或测试。`npm start` / `npm run dev` 使用真实 provider；缺少环境密钥不代表安全，数据库也可能保存密钥和待处理任务。
5. 先复现再改动，按影响选择回归；交付时说明改动、测试、未验证项，以及是否真正部署。

## 文档导航与职责

| 内容 | 入口 |
| --- | --- |
| 项目概况、常用命令 | [README.md](README.md) |
| 代码地图、权限、订单/额度/图片/第三方契约 | [docs/architecture-and-contracts.md](docs/architecture-and-contracts.md) |
| 本地环境、隔离验证、发布与排障 | [docs/agent-operations.md](docs/agent-operations.md) |
| 本次交接基线、验证范围及历史资料索引 | [docs/agent-handoff.md](docs/agent-handoff.md) |
| 后台操作指南的唯一正文 | `backend/content/staff-guide.json`（服务端私有） |
| 后端细节、provider 恢复 | [backend/README.md](backend/README.md) |
| 生产结构及安全运行设置 | [deploy/README.md](deploy/README.md) |
| 数据迁移、独立超管、备份与回退 | [deploy/framework-release.md](deploy/framework-release.md) |
| Agiso 配置与真实联调条件 | [docs/agiso-setup.md](docs/agiso-setup.md) |

`docs/superpowers/` 保存历史需求和实施计划；根目录可能存在未跟踪的 `框架.md`、`模板/` 等用户资料。需分享的正式需求应引用已跟踪文档，不能假定另一台机器拥有这些本地文件。不要自动提交整套设计素材。

## 必须保留的行为边界

- React + TypeScript + Vite 前端、FastAPI 后端、SQLite WAL 单机持久化。应用启动会创建/迁移数据库并启动生成、Agiso worker；不要为诊断随意实例化生产 `Database` 或启动第二套业务服务。
- 角色为 `superadmin`、`org_admin`、`staff`，组织隔离必须由服务端执行。`public` 图库表示组织内共用，不代表匿名公开或跨组织共享。
- 后台 `studio_session` 与访客 `studio_guest` 独立。访客只见订单允许的水印媒体，不能拿到原图、打印文件、内部备注、店铺信息或后台说明；账号/组织停用及订单状态变化要在接口和媒体请求重新校验。
- 当前客户订单为 `workflow_version=3`。`generation_limit` 是可预览份数上限，`final_count` 是最终提交数量，`rerun_limit` 是**整单共用**的重试上限。重复选择占份数，相同头像与同一贴纸版本仅首次生成去重，后续重做和版本选择各自独立。
- 提交恰好 `final_count` 个不同位置；所有在途/未知请求和待确认版本处理完才能提交。“已选 x/y”的分母必须是最终提交数量，手机滚动时保留可见。
- 水印、打印参数、素材和提示词有快照语义；更新图库不改历史订单。只改水印/拼版或恢复后处理，不应重新付费生图。保留原图、旧版本和历史账本。
- 未知外部请求保持占用。有 request ID 时查原请求；只有核对供应商后才可确认结束并释放。不能为通过测试、恢复队列或清空计数而直接篡改任务状态。
- 操作采用 `client_token` 幂等和 `expected_version` 并发检查；响应丢失时沿用原 token 和请求体，不制造重复订单或重复计费。
- 下载由后台主动触发；使用哈希和目录归属保护用户文件。访客页面不自动下载、不把图片持久化到 IndexedDB。
- Agiso 是实体订单选图开户和消息接入，不是物流发货、核销或自动退款工具。配置、授权、推送、SKU 匹配、消息到达、售后应分别验证；模拟通过不等于真实链路接通。

细节和对应文件见[架构与行为契约](docs/architecture-and-contracts.md)。

## 功能与使用说明同步

用户要求：每次修改网站功能时，必须同步修改后台使用说明。

- 唯一正文来源是 `backend/content/staff-guide.json`，网站后台的“使用说明”读取这份内容。
- 新增、修改或删除功能，以及入口、权限、额度、默认值、状态和异常恢复行为变化时，在同一变更中更新对应章节、`updated_at`、`version` 和“说明维护与更新记录”。不能只改日期或写一句“已更新”。
- 按实际实现写说明，使用后台账号能理解的中文；尚未启用或尚需真实联调的功能要明确标注条件，不得写成已经线上接通。
- 不在正文中放密钥、真实客户订单、个人资料或服务器凭证。
- 正文必须保持为服务端私有内容。所有有效后台账号可以读；未登录和只有访客会话时不能读取。不得放进 `frontend/public`、前端源码导入、构建产物或访客缓存。
- 修改认证、文档接口或页面入口时，验证后台可读、访客/未登录拒绝、账号失效后的行为；不能只隐藏菜单。
- 提交前运行 `npm run check:docs`。跨提交审查使用 `npm run check:docs -- --base <实际基础提交或分支>`；该检查只能发现漏改文件，正文是否准确仍需审查。
- 如果改动只涉及内部实现且对用户行为完全无影响，在变更说明中写明原因；同步更新正文中的维护记录说明影响范围，保持检查通过。

## 验证与交付

使用隔离测试数据，按改动运行必要测试和构建。区分本地验证、实际第三方联调和生产部署，不以模拟结果代替线上结果。保留用户已有未提交内容，不覆盖生产数据库。

- 基础检查：`npm test`（说明同步 + 后端 pytest + 前端 Vitest）、`npm run build`。浏览器回归和专项命令见[运维指南](docs/agent-operations.md)。
- 认证/访客/组织改动要测试跨组织拒绝、未登录拒绝、账号失效、受保护媒体和说明接口；涉及布局或互动时补桌面及 390px 手机浏览器验证。
- 队列/重试/售后改动要覆盖并发、重启恢复、重复通知、乱序、未知状态与配额；迁移需要隔离副本演练及原图哈希、历史引用、幂等检查。
- 合并前确定真实基础提交，使用 `npm run check:docs -- --base <base>`；检查合并结果而非只相信分支此前的结果。不要强推主分支。
- 合并或推送代码不等于部署。只有任务包含发布时才改生产；保留数据库、素材和密钥，发布后核对 release/进程/公开页面及权限，不只看 `/api/health`。
- 密钥、令牌、真实订单号、头像、客户消息和数据库备份不可进入 Git、交接文档或测试报告。订单号也是访客入口凭据；日志应去除访客 URL 和授权回调的查询参数。
- 后续变更应同时维护相应架构/运维文档；`CLAUDE.md`、`GEMINI.md` 只保留入口引用，避免各 Agent 各维护一套规则。
