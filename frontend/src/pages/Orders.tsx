import { ImagePreview } from '../components/ImagePreview'
import { previewUrl } from '../lib/preview'
import { useEffect, useState } from 'react'
import { Search, ArrowLeft, Download, Pause, Play, RefreshCw, SlidersHorizontal, FolderCheck, Archive, FolderOpen, Image as ImageIcon } from 'lucide-react'
import { Empty, Modal, PrintFields, Progress, Spinner, Status, useNotice } from '../components/UI'
import { api, post } from '../lib/api'
import { submissionDate, withinPeriod } from '../lib/orders'
import type { Artifact, Item, Order, PrintSettings } from '../lib/types'

function ItemStatus({item}:{item:Item}) {
  if(['queued','running'].includes(item.status)) {
    if(item.processing_stage==='postprocess')return <span className="status status-running"><i/>后处理中</span>
    if(item.fal_status==='IN_QUEUE')return <span className="status status-queued"><i/>FAL 排队中{item.queue_position!=null&&` · 前方 ${item.queue_position} 个请求`}</span>
    if(item.fal_status==='IN_PROGRESS')return <span className="status status-running"><i/>FAL 生成中</span>
    if(item.fal_status==='COMPLETED')return <span className="status status-running"><i/>正在获取成品</span>
  }
  return <Status value={item.status}/>
}

function FalRecovery({item,busy,onRecover,onResolve}:{item:Item;busy:boolean;onRecover:()=>void;onResolve:()=>void}) {
  const [confirmed,setConfirmed]=useState(false)
  return <div className="settings-section">
    <p className="hint">原请求尚未确认结束时会保留并发占用。有请求编号时，先恢复查询原结果。</p>
    {item.recoverable&&<button className="button" disabled={busy} onClick={onRecover}><RefreshCw size={15}/>恢复原请求</button>}
    <label className="check-line"><input type="checkbox" checked={confirmed} onChange={e=>setConfirmed(e.target.checked)}/>我已在 FAL 控制台确认原请求已结束或不存在</label>
    <p className="hint">请先核对平台任务。释放占用不会取消 FAL 任务，也不会重新生图。</p>
    <button className="button" disabled={busy||!confirmed} onClick={onResolve}>释放并发占用</button>
  </div>
}

export interface DeviceState {saved?:boolean;message:string;version?:number;busy?:boolean;error?:boolean}
export function Orders({orders,review=false,onRefresh,onSync,onRedownload,onOpenDirectory,device,initialId,onBack}:{orders:Order[];review?:boolean;onRefresh:()=>void;onSync:(id:string,repair?:boolean)=>Promise<void>;onRedownload:(ids:string[])=>Promise<void>;onOpenDirectory:(id:string)=>Promise<void>;device:Record<string,DeviceState>;initialId?:string|null;onBack?:()=>void}) {
  const [selected,setSelected]=useState<string|null>(initialId||null),[search,setSearch]=useState(''),[filter,setFilter]=useState('all'),[period,setPeriod]=useState(0)
  const [checked,setChecked]=useState<string[]>([]),[downloading,setDownloading]=useState(false),[preview,setPreview]=useState<Order|null>(null)
  useEffect(()=>{if(initialId)setSelected(initialId)},[initialId])
  const filtered=orders.filter(o=>withinPeriod(o.created_at,period)&&(o.name+' '+o.template_codes.join(' ')).toLowerCase().includes(search.toLowerCase())&&(filter==='all'||(filter==='active'?!['completed','complete','ready'].includes(o.status):filter==='issues'?o.failed>0||o.unknown>0||!!o.processing_error:['completed','complete','ready'].includes(o.status))))
  const eligible=filtered.filter(o=>o.download_ready&&!device[o.id]?.busy)
  const checkedOrders=orders.filter(o=>checked.includes(o.id)&&o.download_ready)
  async function downloadChecked(){
    setDownloading(true)
    try{await onRedownload(checkedOrders.map(order=>order.id))}finally{setDownloading(false)}
  }
  if(selected)return <OrderDetail id={selected} onBack={()=>{setSelected(null);onBack?.()}} onRefresh={onRefresh} onSync={onSync} device={device[selected]} review={review}/>
  return <>
    <div className="page-heading"><div><h1>{review?'结果检查':'任务中心'}</h1><p>{review?'逐张查看合成效果，有问题的图片可以单独重跑。':'全部成品完成后自动下载一次；需要再次保存时，勾选订单重新下载。'}</p></div><button className="button" onClick={onRefresh}><RefreshCw size={16}/>刷新</button></div>
    <div className="filter-bar"><div className="tabs">{[['all','全部'],['active','处理中'],['complete','已完成'],['issues','需处理']].map(([id,label])=><button key={id} className={filter===id?'active':''} onClick={()=>setFilter(id)}>{label}{id==='issues'&&orders.some(o=>o.failed||o.unknown)?<i className="notification-dot"/>:null}</button>)}</div><label className="period-filter">提交时间<select aria-label="提交时间范围" value={period} onChange={e=>setPeriod(Number(e.target.value))}>{[[0,'全部时间'],[1,'最近 24 小时'],[3,'最近 3 天'],[7,'最近 7 天'],[30,'最近 30 天']].map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label><label className="search-input"><Search size={16}/><input placeholder="搜索订单或套装编号" aria-label="搜索订单" value={search} onChange={e=>setSearch(e.target.value)}/></label></div>
    {filtered.length?<>
      <div className="toolbar order-download-toolbar"><label className="check-line"><input type="checkbox" aria-label="全选可下载订单" disabled={downloading||!eligible.length} checked={!!eligible.length&&eligible.every(o=>checked.includes(o.id))} onChange={e=>setChecked(prev=>e.target.checked?Array.from(new Set([...prev,...eligible.map(o=>o.id)])):prev.filter(id=>!eligible.some(o=>o.id===id)))}/>全选可下载订单</label><span className="hint">已选 {checkedOrders.length} 个</span><button className="button" disabled={downloading||!checkedOrders.length||checkedOrders.some(o=>device[o.id]?.busy)} onClick={()=>void downloadChecked()}>{downloading?<Spinner/>:<Download size={16}/>}重新下载所选订单</button></div>
      <div className="task-list">{filtered.map(order=><article className="task-row" key={order.id}>
        <input className="task-select" type="checkbox" aria-label={'选择下载 '+order.name} checked={checked.includes(order.id)} disabled={downloading||!order.download_ready||device[order.id]?.busy} onChange={e=>setChecked(prev=>e.target.checked?[...prev,order.id]:prev.filter(id=>id!==order.id))}/>
        <button className="task-main" onClick={()=>setSelected(order.id)}><img className="avatar" src={previewUrl(order.avatar_url)} alt=""/><div><h3>{order.name}</h3><p>{order.template_codes.join(' · ')} <span>·</span> {order.total} 张</p><time dateTime={order.created_at}>提交于 {submissionDate(order.created_at)}</time></div></button>
        <div className="task-progress"><div><Status value={order.paused?'paused':order.status}/><span>{order.completed} / {order.total}</span></div><Progress value={order.completed} total={order.total}/></div>
        <div className="task-device"><FolderCheck size={16}/><span className={device[order.id]?.error?'error-text':''}>{device[order.id]?.message||'尚未保存到本机'}</span></div>
        <div className="task-actions"><button className="order-preview-thumb" disabled={!order.preview_url} aria-label={'预览 '+order.name} title={order.preview_url?'点击查看水印预览':'全部成品完成后显示预览'} onClick={()=>setPreview(order)}>{order.preview_url?<img src={previewUrl(order.preview_url)} alt={order.name+' 水印预览'} loading="lazy"/>:<ImageIcon size={22}/>}</button><button className="button" onClick={()=>setSelected(order.id)}>{review?'检查图片':'查看详情'}</button><button className="button open-order-directory" disabled={!device[order.id]?.saved||device[order.id]?.busy} title="在系统目录窗口中打开此订单的文件夹" onClick={()=>void onOpenDirectory(order.id)}><FolderOpen size={16}/>打开目录</button></div>
      </article>)}</div>
    </>:<Empty title={search?'没有匹配的订单':'这里还没有订单'} description="在工作台上传头像并提交后，处理进度会显示在这里。"/>}
    {preview?.preview_url&&<Modal title={preview.name+' · 水印预览'} onClose={()=>setPreview(null)} wide><ImagePreview className="order-preview-original" src={preview.preview_url} alt={preview.name+' 水印预览'}/></Modal>}
  </>
}

function OrderDetail({id,onBack,onRefresh,onSync,device,review}:{id:string;onBack:()=>void;onRefresh:()=>void;onSync:(id:string,repair?:boolean)=>Promise<void>;device?:DeviceState;review:boolean}) {
  const [order,setOrder]=useState<Order|null>(null),[error,setError]=useState(''),[tab,setTab]=useState(review?'images':'overview'),[item,setItem]=useState<Item|null>(null),[preview,setPreview]=useState<Artifact|null>(null),[print,setPrint]=useState<PrintSettings|null>(null),[busy,setBusy]=useState(''),notice=useNotice()
  useEffect(()=>{
    let active=true
    async function refresh(){try{const next=await api<Order>('/orders/'+id);if(active){setOrder(next);setError('');setItem(current=>current?next.items?.find(i=>i.id===current.id)||null:null)}}catch(e){if(active)setError((e as Error).message)}}
    void refresh();const timer=setInterval(refresh,3000);return()=>{active=false;clearInterval(timer)}
  },[id])
  async function action(path:string,body?:unknown,message='操作已提交') {
    setBusy(path)
    try{await post('/orders/'+id+path,body);const next=await api<Order>('/orders/'+id);setOrder(next);setItem(current=>current?next.items?.find(i=>i.id===current.id)||null:null);onRefresh();notice(message)}catch(e){notice((e as Error).message,'error')}finally{setBusy('')}
  }
  if(!order)return <>{error?<div className="error-banner">{error}</div>:<Spinner/>}<button className="text-button" onClick={onBack}>返回订单列表</button></>
  const files=order.artifacts||[]
  return <><button className="back-button" onClick={onBack}><ArrowLeft size={16}/>返回{review?'结果检查':'任务中心'}</button><div className="page-heading detail-heading"><div className="avatar-name"><img className="avatar large" src={previewUrl(order.avatar_url)} alt={order.name}/><div><h1>{order.name}</h1><p>{order.template_codes.join(' · ')} <span>·</span> 共 {order.total} 张</p><time className="submission-time" dateTime={order.created_at}>提交于 {submissionDate(order.created_at)}</time></div></div><div className="button-group"><button className="button" disabled={!!busy} onClick={()=>action(order.paused?'/resume':'/pause',undefined,order.paused?'任务已继续':'已暂停未开始的任务')}>{order.paused?<Play size={16}/>:<Pause size={16}/>} {order.paused?'继续':'暂停'}</button><button className="button primary" disabled={device?.busy||!files.length} onClick={()=>onSync(id)}>{device?.busy?<Spinner/>:<Download size={16}/>}保存到本机</button></div></div>{error&&<div className="error-banner">{error}</div>}{order.processing_error&&<div className="error-banner">后处理失败：{order.processing_error}。可调整参数后重新排版。</div>}{device?.error&&<div className="notice-banner">{device.message}<button className="text-button" onClick={()=>onSync(id,true)}>修复此订单已管理的文件</button></div>}<div className="order-summary"><div><span>服务器进度</span><strong>{order.completed}<small> / {order.total} 张</small></strong><Progress value={order.completed} total={order.total}/></div><div><span>任务状态</span><Status value={order.paused?'paused':order.status}/>{(order.failed>0||order.unknown>0)&&<small>{order.failed} 张失败 · {order.unknown} 张待确认</small>}</div><div><span>本机保存</span><strong className={'saved-label '+(device?.error?'error-text':'')}>{device?.message||'尚未保存'}</strong><small>服务器完成与本地保存独立记录</small></div></div>{order.unknown>0&&<div className="notice-banner">部分请求结果待确认。请在单张结果中恢复原请求；核对 FAL 控制台确认已结束后，可释放并发占用。重新生成会产生新的 API 费用。</div>}<div className="detail-tabs"><div className="tabs"><button className={tab==='overview'?'active':''} onClick={()=>setTab('overview')}>打印与预览</button><button className={tab==='images'?'active':''} onClick={()=>setTab('images')}>单张结果 <span>{order.total}</span></button></div><div className="button-group"><button className="text-button" onClick={()=>setPrint(order.print_settings)}><SlidersHorizontal size={15}/>重新排版</button><button className="text-button" disabled={!!busy} onClick={()=>action('/watermark',undefined,'水印总览正在更新')}><RefreshCw size={15}/>更新水印</button></div></div>{tab==='images'?<div className="result-grid">{order.items?.map(result=><button className="result-card" key={result.id} onClick={()=>setItem(result)}><div className="checker">{result.result_url?<img src={previewUrl(result.result_url)} alt={result.set_code+' 第 '+(result.position)+' 张'} loading="lazy"/>:<div className="result-pending"><ImageIcon size={28}/><ItemStatus item={result}/></div>}</div><div><span>{result.set_code} · {String(result.position).padStart(2,'0')}</span><ItemStatus item={result}/></div>{result.error&&<p className="error-text">{result.error}</p>}</button>)}</div>:files.length?<><div className="artifact-grid">{files.map(file=><article className="artifact" key={file.id}><button type="button" onClick={()=>setPreview(file)} aria-label={'预览 '+file.path} className={'artifact-preview-trigger'+(file.kind==='overview'?'':' checker')}><img src={previewUrl(file.url)} alt={file.path} loading="lazy"/></button><div><strong>{file.path}</strong><span>{Math.round(file.size/1024)} KB</span></div><a className="text-button" href={file.url} download={file.path}><Download size={15}/>下载此图</a></article>)}</div><div className="export-footer"><p>透明打印文件按套装分别保存，水印总览用于客户确认。</p><a className="button" href={'/api/orders/'+id+'/download.zip'} download><Download size={16}/>打包下载 ZIP</a></div></>:<Empty title="成品正在路上" description="每套图片生成完成后，打印文件会自动显示在这里。"/>}<div className="archive-row"><button className="text-button" disabled={!!busy} onClick={()=>action('/archive',undefined,'归档状态已更新')}><Archive size={15}/>{order.archived?'恢复订单':'归档订单'}</button></div>{preview&&<Modal title={preview.path} onClose={()=>setPreview(null)} wide><ImagePreview className={'artifact-preview'+(preview.kind==='overview'?'':' checker')} src={preview.url} alt={preview.path}/></Modal>}{print&&<Modal title="重新排版" onClose={()=>setPrint(null)}><PrintFields value={print} onChange={setPrint}/><div className="modal-footer"><span className="hint">使用已有成品，不重新调用 AI</span><button className="button primary" disabled={!!busy} onClick={async()=>{await action('/repack',{print_settings:print},'重新排版已提交');setPrint(null)}}>{busy&&<Spinner/>}开始排版</button></div></Modal>}{item&&<Modal title={item.set_code+' · 第 '+(item.position)+' 张'} onClose={()=>setItem(null)} wide><div className="compare-grid"><figure><figcaption>原始头像</figcaption><ImagePreview src={order.avatar_url} alt="原始头像"/></figure><figure><figcaption>固定模板</figcaption><ImagePreview className="checker" src={item.template_url} alt="固定模板"/></figure><figure><figcaption>当前结果</figcaption>{item.result_url?<ImagePreview className="checker" src={item.result_url} alt="当前生成结果"/>:<div className="checker"><div className="empty"><ItemStatus item={item}/></div></div>}</figure></div>{item.error&&<p className="error-banner">{item.error}</p>}{item.fal_request_id&&<><p className="hint fal-request">FAL 请求编号：<code>{item.fal_request_id}</code></p><p><ItemStatus item={item}/></p></>}{item.status==='unknown'&&<FalRecovery key={item.id} item={item} busy={!!busy} onRecover={()=>void action('/items/'+item.id+'/recover',undefined,'正在恢复原请求，不重新生图')} onResolve={()=>void action('/items/'+item.id+'/resolve',{confirmed_ended:true},'已释放占用，可按需重新生成')}/>}<div className="modal-footer"><span className="hint">成功后自动重排所属套装并更新总览。</span><div className="button-group">{item.result_url&&<a className="button" href={item.result_url} download><Download size={15}/>下载单张</a>}{item.raw_available&&!item.remote_reserved&&!['queued','running'].includes(item.status)&&<button className="button" disabled={!!busy} onClick={()=>action('/items/'+item.id+'/reprocess',undefined,'后处理已加入队列，不重新生图')}>仅重试后处理</button>}<button className="button primary" disabled={!!busy||!!item.remote_reserved||['queued','running','unknown'].includes(item.status)} onClick={()=>action('/items/'+item.id+'/rerun',undefined,'单张重跑已加入队列')}><RefreshCw size={16}/>{item.status==='unknown'?'确认重新生成（可能再次计费）':'重新生成这一张'}</button></div></div></Modal>}</>
}
