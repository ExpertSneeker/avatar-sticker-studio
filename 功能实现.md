# 自动生成模板头像

## 整体工作流程

1. 拖入一张/多张头像图片，记录头像图片的文件名（不包括后缀）
2. 为每张头像选择模板，模板库中每12张/套，每套带有一个编号（模板库中设置），每个头像可以选择多个模板
3. 选择模板后，可选择单张尺寸，和生成后拼图后的尺寸（默认A4）。
4. 提交后就开始生成本套图。（要可以设置整个网站全局的图片处理速度（每分钟生成多少张图片））
5. 生成完成后完成拼图，注意要加入单张图重新生成并且生成后重新拼图的功能。
6. 用户选择本地的一个文件夹，生成完成后的拼图写入到文件夹中的 "./[头像图片名]/" 中，拼好图的文件名为 "头像图片名_模板编号_序号(从1开始).png"，透明底。 并且最终输出的文件夹中要有一个带水印的缩略图，水印为半透明文字斜向全图水印，每个账号可以设置单独的水印内容，缩略图为原图1/4像素。
7. 支持断点续传，如果中途关闭了网页，可以核对并下载缺少的，并继续未完成任务。


## 生图

使用OpenAI gpt-image-2 模型 low级别，透明背景的图片，固定提示词（提示词大意就是将图1（模板）中的人物换成图2（头像））