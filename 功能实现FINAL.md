# AI 头像贴纸网站功能实现最终方案

定稿日期：2026-09-06。

本方案以原始《功能实现.md》为主，合并两段微信聊天中的最终约定，以及后续确认的 1K 生图分辨率、水印总览范围和缩放比例，用于后续网站开发与验收。

## 一、网站结构与默认配置

网站面向内部生产人员，包含以下模块：

| 模块 | 功能 |
|---|---|
| 批量工作台 | 上传头像、修改订单名称、逐单或批量选择模板与打印参数、提交任务。 |
| 模板库 | 男孩／女孩分类；每套固定 12 张，具有唯一套装编号；支持导入、排序、替换、预览和上下架。 |
| 任务中心 | 查看生成及下载进度、失败原因；支持暂停未开始的任务、继续任务、单张重跑。 |
| 结果检查 | 对照头像、模板和成品，检查人脸相似度、头身比例、透明边缘和排版。 |
| 账号设置 | 注册／登录、个人水印内容和常用打印参数；各账号的订单与文件相互隔离。 |
| 管理设置 | 管理账号、公共模板、固定提示词、API 配置、全站限速和并发数。 |

首版采用内部邀请注册，不包含商城、支付、物流或微信自动发送。

| 配置项 | 默认值 |
|---|---|
| 生图模型 | `gpt-image-2` |
| 生图质量 | `low` |
| 生图分辨率 | **1024×1024，1K** |
| 输出背景及格式 | 透明背景 PNG |
| 每套模板数量 | 12 张 |
| 单张打印尺寸 | 裁去透明空白后的有效内容长边 **85 mm** |
| 拼图纸张 | A4，210×297 mm |
| 打印分辨率 | 300 DPI |
| 页边距／图片间距 | 均为 10 mm，可调整 |
| 水印预览 | 每个头像一张全部套装总览，宽高各缩小到基准总览的 1/4 |

## 二、完整操作流程

1. **上传头像。** 支持拖入单张或多张 JPG、PNG、WebP，校正照片方向，记录不含后缀的源文件名。默认以该名称作为订单名称；发现重名或非法文件名时，在提交前提示处理，不能静默合并订单。
2. **选择套装。** 每个头像可多选模板套装；支持给选中的多个头像批量应用相同套装。页面显示套装编号及预计生成数量，例如选择 3 套生成 36 张。
3. **设置打印参数。** 可逐单设置或批量应用纸张尺寸、单张长边、页边距与间距。生图像素与打印物理尺寸分别管理。
4. **选择保存目录。** 用户选择本机输出根目录并授予写入权限。取消选择不丢失订单草稿，也不影响已提交的服务器任务。
5. **提交任务。** 上传完成且参数有效的订单进入后台队列。提交后仍可继续添加其他订单，无须等待前面的订单完成。
6. **逐张生图。** 每张独立模板与当前头像组成一次请求；每套共 12 个独立子任务。固定图 1 为模板、图 2 为头像。
7. **后处理与拼图。** 校验图片可解码及透明通道，按需抠图；亮度和色彩调整作为默认关闭的可选项。按套装分别排版并自动分页。
8. **输出与检查。** 各套打印文件完成后可陆续保存；该头像全部套装完成后，生成唯一的水印总览。页面分别显示“服务器已生成”和“本机已保存”。
9. **单张重跑。** 指定某张成品重新生成，其他单张不重新调用 AI。成功后自动重排所属套装、更新总览，并同步更新本地对应文件；失败时保留上一版本供查看。

### 生图请求约定

使用 Image API 的图片编辑接口 `POST /v1/images/edits`：

- `model=gpt-image-2`
- `quality=low`
- `size=1024x1024`
- `background=transparent`
- `output_format=png`
- `n=1`
- 输入图片按“模板、头像”顺序传入；省略 `input_fidelity`。

截至 2026-09-06 核对的官方文档，`gpt-image-2` 的透明背景功能处于预览阶段，图像输入自动使用高保真处理。[OpenAI 图片生成指南](https://developers.openai.com/api/docs/guides/image-generation)

固定提示词围绕以下要求编写并保存版本：

> 图 1 是贴纸模板，图 2 是人物身份参考。将模板中的头部替换为图 2 中的人物，保留可辨识的五官、发型和脸型。保持模板的身体、服饰、姿势、道具和画风，调整头部角度与衔接，使比例自然。完整保留贴纸主体，不裁断，不增加额外人物、文字、水印或背景，输出透明背景的单张贴纸。

模板与提示词的修改只影响新任务；已提交任务使用提交时的版本。

## 三、排版、命名与水印

### 打印文件

**每个套装独立拼图，不跨套混排。** 文件保存在用户选择的根目录下：

```text
[输出根目录]/
└── [头像图片名]/
    ├── [头像图片名]_[套装编号A]_1.png
    ├── [头像图片名]_[套装编号A]_2.png
    ├── [头像图片名]_[套装编号B]_1.png
    ├── [头像图片名]_[套装编号B]_2.png
    └── [头像图片名]_水印总览.png
```

- 文件名中的“模板编号”指**整套编号**；页序号在每套内从 **1** 开始。
- 打印 PNG 保持透明背景，不附加水印、订单文字或裁切线。
- A4 在 300 DPI 下输出 **2480×3508 px**，并写入正确的 DPI 元数据；85 mm 对应约 **1004 px**。
- 按透明内容边界等比缩放，允许旋转以便排版，不拉伸人物、不裁切主体。
- 默认参数可以容纳 6 个 85 mm 方形区域；实际页数由图片形状和排版计算，不能为了凑数量缩小单张尺寸。
- 单张结果保存在服务器，用于重跑、重新排版和单独下载；默认本地目录只保存打印拼图和一张水印总览。

### 唯一水印总览

- 汇总该头像全部套装，按套装顺序、套内固定顺序排列，人物保持正向。
- 每套采用 **4 列×3 行**，多套纵向接排。
- 以原始 1024×1024 成品画布作为基准单元，宽高各缩至 1/4 后拼接；每张预览单元为 256×256，单套预览区域为 1024×768。
- 预览采用白底，添加重复平铺的斜向半透明文字水印，覆盖全图。
- 默认水印角度 45°、不透明度 25%；内容来自该账号设置，未设置时使用账号显示名。
- 修改水印只重新生成总览，不重新生图或拼版。

## 四、后台、限速与断点恢复

### 服务端处理

采用浏览器工作台、Python 图像处理服务、持久化任务队列及文件存储。原图、模板版本、生成结果、处理参数、失败信息和文件清单均保存在服务端。

前后端需提供上传及恢复、模板查询、任务提交、进度查询、暂停／继续、单张重跑、重新排版、结果清单和文件下载接口。所有任务与文件接口校验账号归属，API 密钥不发送到浏览器。

### 全站限速

- 管理员设置“每分钟最多发起多少次单张生图请求”，同时设置最大在途请求数。
- 初始默认 **5 次／分钟、最多 2 个在途请求**，部署时按真实 API 配额校准。
- 全站所有账号、后台进程、单张重跑和重试共用限速器。
- 限制的是请求启动速度，不能承诺每分钟一定完成同样数量。
- 生图与抠图分别限速，不能直接沿用脚本的抠图 QPS 作为 OpenAI 生图限额。
- 429 等可重试错误退避后重试；参数、鉴权和内容拒绝错误明确显示原因，不无限重试。

### 断点恢复

- 已完成上传和生图的内容不重复处理；上传中断后恢复上传进度，必要时让用户重选同一源文件。
- 关闭网页不取消已提交的后台任务；重新打开后从服务器恢复状态。
- 服务器重启后恢复待处理任务。已经发出但结果不明的 API 请求标为“结果待确认”，不能仅因本地超时就认定未生成并自动重复付费。
- 文件清单记录相对路径、结果版本、大小和校验值。本地补齐时比较实际文件，缺失、损坏和旧版本均需更新。
- 单张重跑引起页数变化时，先完整保存新版本，再清理清单中属于该任务的淘汰页；其他本地文件保持不动。
- 首版不自动删除未归档任务的原图和结果，保证恢复与重新排版可用。

### 本地目录限制

桌面 Chrome／Edge 优先通过 File System Access API 保存到用户选择的目录。该能力需要安全上下文及用户授权；其他浏览器提供保留相同目录结构的 ZIP 下载。[Chrome 文件系统访问文档](https://developer.chrome.com/docs/capabilities/web-apis/file-system-access)

网页关闭或电脑离线期间，服务器继续生成，但不能继续写入本机目录；重新打开网页后检查目录权限并补齐。权限失效或磁盘写入失败时，保留服务器结果并显示“待保存”，不能显示为全部完成。

### 现有脚本复用

参考目录：[抠图Test](/Users/magnus/Desktop/抠图Test)。

| 脚本 | 复用与调整 |
|---|---|
| `Step1 抠图_优化.py` | 参考上传预处理、限流、异步查询、下载与失败记录；提取为独立抠图服务。 |
| `Step2 亮度调整.py` | 保留透明通道的亮度处理，改为可选步骤。 |
| `Step3 拼图_透明底版 current.py` | 复用透明裁边、等比缩放与分页思路；替换目录扫描、固定参数及文件命名，补齐 DPI 和透明合成验证。 |
| `Step4 色彩平衡.py` | 改为可选打印调色预设。 |

## 五、验收与文档交付

必须覆盖以下场景：

1. 多头像选择不同套装，生成张数准确，订单、套装与文件归属正确。
2. 单张重跑只产生一次对应的新请求，成功后自动更新所属套装及唯一总览。
3. 多账号同时运行时，全站请求速度不超过配置；暂停和继续不会重复提交已完成图片。
4. 上传、生成、下载三个阶段分别断线或关闭网页，均能恢复正确状态。
5. 本地存在同名文件、旧版本、损坏文件，或目录权限失效、磁盘空间不足时，能准确识别并恢复。
6. 打印 PNG 透明、尺寸及 DPI 正确；实打检查 85 mm 长边、人物清晰度与比例。
7. 每个头像仅一张水印总览，包含全部成品、人物正向、宽高缩放比例正确，且不同账号使用各自水印。
8. 修改尺寸或水印只重新处理已有图片，不额外调用生图；修改模板不改变旧任务使用的模板版本。

本次交付仅新建 `功能实现FINAL.md`，保留原始 `功能实现.md`。写入后回读核对以上约定，不进行网站开发、API 付费测试或部署。以上验收场景用于后续网站实现，并不表示这些功能已经开发或测试完成。
