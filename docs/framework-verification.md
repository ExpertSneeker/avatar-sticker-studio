# 新框架验收与发布记录

日期：2026-09-11。生产代码提交：`0df4af6f2cdf641608e0f2c8073efdbe68b6a754`，从 `eb627b8` 升级。代码已合并至 `codex/avatar-sticker-studio`。本文件是发布后的记录，不改变已发布程序。

## 实现与审查

组织隔离及账号角色、独立超管命令、取消积分限制、多头像与独立份数、首次去重、重做版本、访客水印流程、定量提交锁定、回收站及解锁、批量水印、自动拼版和后台手动交付均已实现。

四个模块已独立审查。审查发现的历史未知请求释放、取消订单统计、取消后抠图提交、未解决抠图占用提交、丢失响应后的重试死锁、迁移审计遗漏当前图库均已修复并复审通过，无待处理 P1/P2。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| 完整后端测试 | 189 项通过 |
| 前端单元测试 | 49 项通过 |
| 完整 Chrome 浏览器回归 | 28 项通过 |
| 最后修改涉及的浏览器专项 | 2 项通过 |
| 前端构建及主工作目录合并后构建 | 通过 |
| 生产数据库隔离迁移演练 | 通过，重复执行幂等 |
| 生产迁移后历史记录及原图校验 | 通过 |

后端有 3 个已有依赖弃用／大尺寸图片警告。自动测试使用隔离临时数据和模拟生图服务，没有真实生图费用。

浏览器覆盖手机尺寸 390×844 和桌面 1440×1000；已检查上传、多头像、重复份数、首次去重、重做保留旧版、严格定量提交、水印总览、取消恢复、解锁、批量水印、组织管理、丢失成功响应后的幂等重试。目录授权通过 OPFS 测试夹具验证，ZIP 备用已覆盖。验证浏览器为 Chrome，未宣称 Safari 或真实打印机验收。

访客检查未发现后台／原图请求、自动下载、IndexedDB 图片保存、页面异常或横向溢出。提交后选图卡片清空，仅显示水印总览。

## 生产发布

- 新 release：`/opt/avatar-sticker-studio/releases/0df4af6`。归档与全部 176 个源文件／构建文件已校验哈希。
- 切换前确认无排队、在途或未知占用任务，并停止唯一服务。
- 完整私有备份：`/opt/avatar-sticker-studio/backups/framework-20260911-065537`，所有文件逐字节哈希比对通过，保留旧 release `eb627b8`。
- 在生产原数据库执行组织迁移，没有使用本地数据库覆盖。137 个原始资源、3 个历史订单、36 个任务、全部素材版本、配置和历史账本保持完整。
- 创建独立超级管理员 `siteadmin`，随机密码保存在本机私有文件中，未写入 Git 或本记录。`magnus` 为组织管理员，原成员归属同一组织。
- `current`、运行进程工作目录及 `REVISION` 均验证为新 release；本机及公网服务可用。
- 线上浏览器验证超管登录及组织页面、手机访客入口、匿名后台／原图拒绝访问；登录后抽查原图响应哈希与迁移前一致。没有创建测试生产订单或发起真实生图。

## 证据与回退

本机详细日志及截图：`/tmp/sticker-framework-evidence/`、`/tmp/avatar-studio-fal-qa/`。本机子任务审查记录：`.superpowers/sdd/2026-09-11-organization-guest/`。

回退遵循 [发布与备份边界](../deploy/framework-release.md)：新框架产生业务数据后，不可直接以旧数据库覆盖。应保留新数据并修复，或单独核对迁移；旧代码不理解新角色和订单模型。
