# Agent 交接记录

记录日期：2026-09-26。本文件记录此次交接的证据范围；长期规则以 [AGENTS.md](../AGENTS.md) 为入口，业务细节见[架构与行为契约](architecture-and-contracts.md)，命令见[运维指南](agent-operations.md)。接手时重新查看 Git 和运行环境，不把此处快照当作实时状态。

## 主分支与本次范围

- 核对并 fetch 后，`origin/main` 与原工作分支 `codex/avatar-sticker-studio` 均为 `e984fdb`；本地 `main` 原为 `1d5d594`，落后 74 个提交，已快进并切换到 `main`。
- `codex/pdd-agiso`（`881713e`）和 `codex/public-stickers`（`eb627b8`）的提交已经包含在该功能基线。没有将旧 worktree 的文件覆盖到新代码，也没有清理这些工作区。
- 本次追加跨 Agent 入口、架构契约、运维和交接文档；修正 README 中已过时的每份重做额度、固定 12 张模板、分类及历史订单说明，并给旧发布/联调记录标注范围。
- 后台使用说明更新版本、日期和维护记录，并修正“订单号无效”排查中旧的等待成团说法；当前开户以付款后的订单确认通知为准。本次没有修改应用功能、安装或升级依赖、操作生产数据、发布生产或发送客户消息。
- 本次开始时根目录已有 12 张 `test/sticker-*.png` 的未提交删除，以及未跟踪的 `框架.md`、`模板/`；均保留在用户工作区，不属于交接提交。后续 Agent 不要把这些现状误判成本次文档改动或擅自恢复/清理。

文档提交的具体 SHA 请从 `git log -1 --oneline` 获取，不在同一提交内硬编码自身哈希。`main` 的推送和生产发布是两个独立动作。

## 本地验证证据

| 检查 | 本次结果和限制 |
| --- | --- |
| `npm test` | 后端 254 项、前端 56 项通过；包含后台说明同步检查 |
| `npm run build` | TypeScript 与 Vite 构建通过 |
| `npm --prefix frontend run test:e2e -- e2e/staff-guide.spec.ts` | 2 项通过：后台读取/搜索/退出清除，访客无入口且直接请求拒绝 |
| 说明正文与交接文档 | 核对 JSON 结构、版本/更新记录、Markdown 本地链接和跨提交说明同步 |
| 完整浏览器回归 | 本次未运行；不能使用历史测试总数代替当前验收 |
| 真实 FAL / Yezi / Agiso、真实打印、当前线上运行版本 | 本次未验证 |

测试使用临时数据和模拟生成服务；浏览器专项显式清除外部服务环境凭证。后端有 3 个警告：Starlette/httpx 与 AnyIO 弃用提示，以及 600 DPI 大图测试中的 Pillow 尺寸警告；本次未修改依赖或图片阈值。

## 后续操作前要现场确认

1. 当前 Git 分支、远端进度、未提交内容及其他 Agent/worktree，避免覆盖并行工作。
2. 本地 `.env`、数据目录和服务进程的归属；只核对所需配置，不把实际值、订单号、头像或密钥输出到日志。启动器可能启动真实任务。
3. 生产 `current`、`REVISION`、systemd 进程目录和持久数据目录。仓库最新提交或 HTTP 200 本身不能证明它已经上线。
4. Agiso 应用权限、店铺授权有效期、服务续费、发送助手在线、商品/SKU 规则及开关。文档中的 2026-09-18 服务状态不是当前事实。
5. 有关真实交易的判断必须分别看到接收通知、开户、消息到达及售后结果；`Goods/List` 的应用权限错误不等于没有商品。
6. 变更状态机或迁移时，重新核对未知外部请求、原图与历史引用、整单配额、售后保护、访客权限和下载冲突保护。

## 历史资料怎么用

| 资料 | 用途与局限 |
| --- | --- |
| [完整框架需求](superpowers/specs/2026-09-11-framework-source.md) | 已跟踪的原始需求，不依赖本机未跟踪 `框架.md`；后来经用户确认的改动以当前契约为准 |
| [组织/访客实施计划](superpowers/plans/2026-09-11-organization-guest.md) | 解释组织、不可变版本、访客和迁移背景；其中按份重做等早期设计已被后续整单规则替代 |
| [Agiso 实施计划](superpowers/plans/2026-09-14-pdd-agiso.md) | 了解接入设计；topic、字段兼容和恢复细节需看当前源码 |
| [公共图库发布记录](public-library-release.md) | 旧图库阶段的迁移与验收；旧积分/共享重做模型不是当前 v3 |
| [框架验收记录](framework-verification.md) | 2026-09-11 的发布、备份与测试证据；不能证明当前生产提交或备份可用 |
| [早期验证记录](verification.md) | 初期测试范围和产物；不代表今天的完整回归结果 |
| [设计记录](design/design-system.md) | 初版视觉方向与历史核对；当前导航、订单弹窗和手机行为以现代码及浏览器为准 |

新增交接记录时注明日期、代码基线、实际命令/结果和未验证范围；不要把待办、模拟验证或另一位 Agent 的口头报告写成完成事实。
